{
  "name": "proofreading-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered proofreading assistant for researchers",
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix server\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix frontend && npm run build --prefix server",
    "build:frontend": "npm run build --prefix frontend",
    "build:server": "npm run build --prefix server",
    "install:all": "npm install && cd frontend && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
